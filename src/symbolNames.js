const _steps = Symbol('steps');
const _links = Symbol('links');
const _sprite = Symbol('sprite');
const _workflow = Symbol('workflow');
const _render = Symbol('render');
const _isDragging = Symbol('isDragging');
const _hasLinkReject = Symbol('hasLinkReject');

export { _steps, _links, _sprite, _workflow, _render, _isDragging, _hasLinkReject }
